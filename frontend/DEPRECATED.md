# ⚠️ DEPRECATED

This per-inator frontend is **deprecated** as of Feb 2026.

All frontend code has been consolidated into the **unified frontend** at the platform root:

```
inator/frontend/src/modules/rma/
```

The unified frontend is served through the Caddy gateway at `http://localhost:8080`.

This directory is retained for reference only and will be removed in a future release.
